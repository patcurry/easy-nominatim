{
  "name": "easy-nominatim",
  "version": "0.0.1",
  "description": "helper functions for open street maps nominatim",
  "main": "build/easy-nominatim.js",
  "scripts": {
    "dev": "watch 'npm run build' src",
    "compile": "babel --presets env, stage-0 -d build/ src/",
    "build": "babel src -d build",
    "poly": "babel --plugins transform-regenerator src build",
    "test": "mocha --require babel-polyfill --compilers js:babel-register",
    "test:watch": "npm test -- --watch",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/patcurry/easy-nominatim.git"
  },
  "keywords": [
    "osm",
    "open",
    "street",
    "maps",
    "nominatim"
  ],
  "author": "Pat Curry",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/patcurry/easy-nominatim/issues"
  },
  "homepage": "https://github.com/patcurry/easy-nominatim#readme",
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-plugin-transform-regenerator": "^6.22.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-polyfill": "^6.23.0",
    "babel-preset-env": "^1.2.2",
    "babel-preset-stage-0": "^6.22.0",
    "babel-register": "^6.24.0",
    "chai": "^3.5.0",
    "leaflet": "^1.0.3",
    "mocha": "^3.2.0",
    "watch": "^1.0.2"
  }
}
